-- 1
ALTER TABLE PRODUCT ADD ID_CATEGORY INTEGER;

ALTER TABLE PRODUCT
ADD CONSTRAINT FK_PRODUCT_CATEGORY
FOREIGN KEY (ID_CATEGORY)
REFERENCES CATEGORY (ID_CATEGORY)

-- 2
ALTER TABLE PRODUCT ADD ID_SUPPLIER INTEGER;

ALTER TABLE PRODUCT
ADD CONSTRAINT FK_PRODUCT_SUPPLIER
FOREIGN KEY (ID_SUPPLIER)
REFERENCES SUPPLIER (ID_SUPPLIER)

-- 3
ALTER TABLE SALES ADD ID_EMPLOYEE INTEGER;

ALTER TABLE SALES
ADD CONSTRAINT FK_SALES_EMPLOYEE
FOREIGN KEY (ID_EMPLOYEE)
REFERENCES EMPLOYEE (ID_EMPLOYEE)

-- 4
CREATE TABLE CLIENT (
    ID_CLIENT INTEGER PRIMARY KEY NOT NULL,
    NAME_CLIENT VARCHAR(100)
)

ALTER TABLE SALES ADD ID_CLIENT INTEGER;

ALTER TABLE SALES
ADD CONSTRAINT FK_SALES_CLIENT
FOREIGN KEY (ID_CLIENT)
REFERENCES CLIENT (ID_CLIENT)

-- 5
CREATE TABLE SALES_ITEMS (
    ID_SALE INTEGER,
    ID_PRODUCT INTEGER,
    QUANTITY INTEGER,
    UNIT_PRICE NUMERIC(12,2),

    CONSTRAINT FK_ITEMS_PRODUCTS FOREIGN KEY (ID_PRODUCT)
    REFERENCES PRODUCT (ID_PRODUCT),
    CONSTRAINT FK_ITEMS_SALES FOREIGN KEY (ID_SALE)
    REFERENCES SALES (ID_SALE)
)