-- 1
ALTER TABLE PRODUCT ADD MIN_STOCK INTEGER
INSERT INTO PRODUCT (ID_PRODUCT, DESCRIPTION, ID_CATEGORY, ID_SUPPLIER)
 VALUES (3, 'SNIKERS', 1, 1)
INSERT INTO PRODUCT (ID_PRODUCT, DESCRIPTION, ID_CATEGORY, ID_SUPPLIER)
 VALUES (4, 'SSD', 2, 2)
UPDATE PRODUCT
 SET DESCRIPTION = 'SNIKERS',
     MIN_STOCK = 1
 WHERE ID_PRODUCT = 3
UPDATE PRODUCT
 SET DESCRIPTION = 'SDD T',
     MIN_STOCK = 1
 WHERE ID_PRODUCT = 4

-- 2
SELECT FIRST 1 *
FROM PRODUCT
WHERE ID_CATEGORY IN (1,2)
ORDER BY DESCRIPTION ASC

-- 3
SELECT *
FROM SALES_ITEMS SI
WHERE (SI.QUANTITY * SI.UNIT_PRICE) > 5
ORDER BY SI.QUANTITY DESC, SI.UNIT_PRICE ASC

-- 4
SELECT FIRST 2 *
FROM EMPLOYEE E
WHERE E.DATE_HIRE >= '2025-08-27'
ORDER BY E.WAGE DESC, E.NAME_FULL ASC

-- 5
DELETE FROM SALES_ITEMS SA
WHERE SA.QUANTITY < 2 AND SA.UNIT_PRICE < 3

SELECT FIRST 3 *
FROM SALES_ITEMS SAA
WHERE SAA.ID_PRODUCT <> 3
ORDER BY SAA.UNIT_PRICE DESC